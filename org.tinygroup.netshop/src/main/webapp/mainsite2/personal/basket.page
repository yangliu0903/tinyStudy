#callService("processModelService" "requestName=RelationModel_relationQuery_basketRelation_queryProductsOfBasket&userUuid=0105f0751c2f4bea888fc0508a8aea2b" "modelProcessResult" "basketQueryResult")
#setTitle("购物车管理")
#@boxWithDate("购物车管理" "订单管理")
	#if($basketQueryResult.beans.size()>0)
		#foreach( $record in $basketQueryResult.beans)
		
			#callService("processModelService" "requestName=EntityModel_table_picture_queryTargetPictureUrl&pictureUuid=$record.pictureUuid" "modelProcessResult" "pics")
				#if( $pics.beans.size() == 0)
					#set($pic = $pic1 )
				#else
					#set($pic = $pics.beans.get(0) )
				#end
			#basketItem("$pic.pictureLink" "$record.productName" "$record.productName" "$record.productNorm" $record.productNum $record.unitPrice 0 $record.productDiscount)
		#end
		<div class="tr pr20">
			<input type="button" class="button-2" id="btn4" value="购买">
		</div>
	#end
#end

