
#if($modelProcessResult.totalSize>300)
<div style="float:left;border:1px solid;margin:5px 5px;width:$cardWidth;">
	命中数据条数为$modelProcessResult.totalSize，数量太多，不适合采用Card方式显示，请通过查询使得要显示的数据减少
</div>
#else
#foreach($bean in $modelProcessResult.beans)
<div style="float:left;border:1px solid;margin:5px 5px;width:$cardWidth;">
	#displayViewGroup($operationDefine $bean "labelMode")
</div>
#end
#end
