## 逻辑运算
逻辑运算
#set(a = 1)
#set(b = 2)
${a == 1 && b == 2}

## 自增 自减
自增 自减
${++a}
${a--}
${a}

## 算数运算
算数运算
#set(a = 2)
${a+1}
${a-1}
${a*2}
${a/4.0}
${a%2}

## 移位运算
移位运算
#set(a = 3) ## 二进制为  0011
${a << 1}
${a >> 1}
${a >>> 1}
${~a}
#eol

## 比较运算
比较运算
#set(a = 3)
#set(b = 4)
${a != b}
${a == b}
${a >= b}
${a < b}

## 三元表达式
三元表达式
#set(a = 3)
#set(b = 4)
${(a == b)?:b}
${(a != b)?a:b}
${(a != b)?:b}


## 索引表示法
#set(foo = ["1","2","3"])
#for(i:foo)
	${i}
#end




